package components;

public class NonClickable extends Component{
    // Class is empty for a reason:
    // It is only there, so I can add it do game-objects that should not be clickable in the
    // editor like the gizmo arrows. You can only add this component in the code and I wouldn't
    // recommend it because the only thing it does is, as I said, you can't move the game-object or
    // change his variables in the editor which is pretty dumb.
}
